version: '3.8'

services:
  my-node-app:
    build: .
    container_name: web_trainer_app
    restart: always
    network_mode: host  # <--- Использовать сеть хоста напрямую
    volumes:
      - ./data:/app/data:z
